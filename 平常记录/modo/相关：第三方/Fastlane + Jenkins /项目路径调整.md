1. 目前工程很多地方是 绝对路径
   ```
   #!/bin/bash
   
   # 系统变量，保持原样
   # 注意：在 Xcode Run Script Phase 中，$SRCROOT 和 $BUILD_DIR 会自动被赋值
   echo "------------------- 使用 cd + pwd -------------------"
   echo "🀄️🀄️🀄️ SRCROOT (absolute) = ${SRCROOT}"
   echo "🀄️🀄️🀄️ BUILD_DIR (absolute) = ${BUILD_DIR}"
   
   # 需要解析的相对路径
   PODS_ROOT="${SRCROOT}/../../../build/ios/proj/Pods"
   BUILD_PATH="${SRCROOT}/../../../build/ios/proj"
   
   # 使用 cd + pwd 解析绝对路径
   PODS_ROOT_ABS=$(cd "$PODS_ROOT" && pwd)
   BUILD_PATH_ABS=$(cd "$BUILD_PATH" && pwd)
   echo "🀄️🀄️🀄️ PODS_ROOT (absolute) = $PODS_ROOT_ABS"
   echo "🀄️🀄️🀄️ Build Path (absolute) = $BUILD_PATH_ABS"
   
   echo ""
   echo "------------------- 使用 realpath -------------------"
   echo "🀄️🀄️🀄️ SRCROOT (absolute) = ${SRCROOT}"
   echo "🀄️🀄️🀄️ BUILD_DIR (absolute) = ${BUILD_DIR}"
   
   # 使用 realpath 解析绝对路径
   PODS_ROOT_REAL=$(realpath "$PODS_ROOT")
   BUILD_PATH_REAL=$(realpath "$BUILD_PATH")
   echo "🀄️🀄️🀄️ PODS_ROOT (absolute) = $PODS_ROOT_REAL"
   echo "🀄️🀄️🀄️ Build Path (absolute) = $BUILD_PATH_REAL"
   
   
   ```

   打印出来

   ```
   
   Showing All Messages
   ------------------- 使用 cd + pwd -------------------
   
   🀄️🀄️🀄️ SRCROOT (absolute) = /Users/lin/Documents/GardenGlobal/native/engine/ios
   
   🀄️🀄️🀄️ BUILD_DIR (absolute) = /Users/lin/Library/Developer/Xcode/DerivedData/GardenGlobal-fkphtugnludcybhheofbywtavobd/Build/Products
   
   🀄️🀄️🀄️ PODS_ROOT (absolute) = /Users/lin/Documents/GardenGlobal/build/ios/proj/Pods
   
   🀄️🀄️🀄️ Build Path (absolute) = /Users/lin/Documents/GardenGlobal/build/ios/proj
   
   ------------------- 使用 realpath -------------------
   
   🀄️🀄️🀄️ SRCROOT (absolute) = /Users/lin/Documents/GardenGlobal/native/engine/ios
   
   🀄️🀄️🀄️ BUILD_DIR (absolute) = /Users/lin/Library/Developer/Xcode/DerivedData/GardenGlobal-fkphtugnludcybhheofbywtavobd/Build/Products
   
   🀄️🀄️🀄️ PODS_ROOT (absolute) = /Users/lin/Documents/GardenGlobal/build/ios/proj/Pods
   
   🀄️🀄️🀄️ Build Path (absolute) = /Users/lin/Documents/GardenGlobal/build/ios/proj
   
   
   ```

   

${SRCROOT}

${BUILD_PATH}

**cd** /Users/lin/Documents/GardenGlobal/build/ios/proj

---

#### 创建iOS项目拖引擎进入iOS项目



1. 复制cocos中.h文件

```
#!/usr/bin/env python3
import os
import shutil

# ----------------------
# 配置区
# ----------------------
# Cocos Creator 源码路径
COCOS_SRC_ROOT = "/Applications/Cocos/Creator/3.8.6/CocosCreator.app/Contents/Resources/resources/3d/engine/native/cocos"

# V8 引擎源码路径（根据你 Cocos 安装实际路径调整）
V8_SRC_ROOT = "/Applications/Cocos/Creator/3.8.6/CocosCreator.app/Contents/Resources/resources/3d/engine/native/external/v8/include"

# 目标 include 目录
DST_ROOT = os.path.join(os.getcwd(), "CCNeedFrameworks/include")

# ----------------------
# 核心函数：拷贝 .h 和 .hpp 文件
# ----------------------
def copy_headers(src_root, dst_root):
    """
    递归拷贝 src_root 下的所有 .h/.hpp 文件到 dst_root，保持原目录结构。
    """
    if not os.path.exists(src_root):
        print(f"[WARN] Source root not found: {src_root}")
        return

    for root, dirs, files in os.walk(src_root):
        for file in files:
            if file.endswith((".h", ".hpp")):
                src_file = os.path.join(root, file)
                rel_path = os.path.relpath(src_file, src_root)
                dst_file = os.path.join(dst_root, rel_path)
                dst_dir = os.path.dirname(dst_file)
                os.makedirs(dst_dir, exist_ok=True)

                # 增量拷贝
                if not os.path.exists(dst_file) or os.path.getmtime(src_file) > os.path.getmtime(dst_file):
                    shutil.copy2(src_file, dst_file)
                    print(f"[COPY] {rel_path}")
                else:
                    print(f"[SKIP] {rel_path} (up-to-date)")

# ----------------------
# 主程序
# ----------------------
if __name__ == "__main__":
    print("=== Copy all Cocos + V8 headers start ===")
    print(f"Cocos SRC_ROOT = {COCOS_SRC_ROOT}")
    print(f"V8 SRC_ROOT    = {V8_SRC_ROOT}")
    print(f"DST_ROOT       = {DST_ROOT}\n")

    # 拷贝 Cocos 头文件
    copy_headers(COCOS_SRC_ROOT, DST_ROOT)

    # 拷贝 V8 头文件
    copy_headers(V8_SRC_ROOT, DST_ROOT)

    print("\n=== Copy finished ===")
    print("请在 Xcode 中将 CCNeedFrameworks/include 添加到 Header Search Paths（非递归）")


```

2. path中各种修改路径,其中记得去 process 将宏相关的都copy

   