>  **需求链接:**[**https://modoglobal.feishu.cn/docx/ISj3dyF3RoYe7KxSuq9c9QOwnfd**](https://modoglobal.feishu.cn/docx/ISj3dyF3RoYe7KxSuq9c9QOwnfd)

<img src="/Users/lin/Library/Application Support/LarkShell/sdk_storage/5bbd7ac97bce8f6f1f6594334b1ce40a/resources/images/img_v3_02r5_64b041c1-1c04-4a18-a485-ba204da465eg.jpg" alt="img_v3_02r5_64b041c1-1c04-4a18-a485-ba204da465eg" style="zoom:50%;" />

<img src="/Users/lin/Library/Application Support/LarkShell/sdk_storage/5bbd7ac97bce8f6f1f6594334b1ce40a/resources/images/img_v3_02r5_e4b3b127-f932-46b7-84d4-04aab5a4d53g.jpg" alt="img_v3_02r5_e4b3b127-f932-46b7-84d4-04aab5a4d53g" style="zoom:50%;" />

### **灵动岛(实时屏幕)开发问题:**

1. 主动开启灵动岛方案 (通知/游戏中点击/进入后台或者关闭屏幕)
2. 实时屏幕数量(一个/多个)
3. 更新   倒计时结束 (远程服务端还是SDK 主动更新)    ( APP 杀死后,只能服务端更新) 
   (状态值更新-->SDK需要做多语言处理    还是全数据更新-->多语言)
4. 停止  去采摘点击后(客户端主动end)   ( APP 杀死后,只能服务端更新) 
5. APP杀死后,灵动岛是否随着关闭

---

其他项目加入灵动岛操作:
```objc
1://远程路径 
 pod 'ActivitySDK',:git =>"ssh://git@192.168.13.252:8022/modo_ios/activitysdk.git"
 pod 'ActivityUI',:git => "ssh://git@192.168.13.252:8022/modo_ios/activityui.git"	
   
   
   //本地路径
 # pod  'ActivityUI', :path => '../LiveActivity/ActivityUI'
   pod  'ActivitySDK', :path => '/Users/lin/Desktop/gameProject/LiveActivity/ActivitySDK'
   
     
  //扩展Extension 添加SDK 
   target 'HZWLiveActivityExtension' do
  #    inherit! :search_paths
  pod  'ActivityUI',:git => "ssh://git@192.168.13.252:8022/modo_ios/activityui.git"
#  pod  'ActivityUI', :path => '../LiveActivity/ActivityUI'
end
   
2.//# 在pod 中强制更改为动态,目的暴露 Swift API(添加在能力SDK(被设置覆盖成为了静态库))
    
    /*为什么?  
    在pod中 下面RN库 覆盖全局 都是静态库,而swift开发的SDK,OC 是无法在静态库中引入 #import <ActivitySDK/ActivitySDK-Swift.h>   
    没有这个,我发使用SDK 所以强制动态,但是纯能力SDK 引入后,还是无法使用.
    需要在主工程中 动态引入  
   config = use_native_modules!
  use_react_native!(:path => config["reactNativePath"],:hermes_enabled => true)
    */
pre_install do |installer|
  installer.pod_targets.each do |pod|
    if pod.name == 'ActivitySDK'
      puts "👉 [pre_install] 强制将 #{pod.name} 设置为动态 framework"
      def pod.build_type
        Pod::BuildType.dynamic_framework
      end
    end
  end
end
          
//权限支持  考虑是不是要与通知权限一起判断
3.主APP
	<key>NSSupportsLiveActivities</key>
	<true/>

4.灵动岛target 最低支持 iOS 16.1
5.灵动岛UI 替换SDK中UI

 import ActivityUI
@main
struct HuaYuanLABundle: WidgetBundle {
    var body: some Widget {
        ActivityWidgetBundle().body
    }
}

6.添加共享group,(PCH 中已经获取group.(bundle id))
```

