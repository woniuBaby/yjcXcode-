1.pod 记录内容

```objc
1://远程路径 
 pod 'ActivitySDK',:git =>"ssh://git@192.168.13.252:8022/modo_ios/activitysdk.git"
 pod 'ActivityUI',:git => "ssh://git@192.168.13.252:8022/modo_ios/activityui.git"	
   
   
   //本地路径
 # pod  'ActivityUI', :path => '../LiveActivity/ActivityUI'
   pod  'ActivitySDK', :path => '/Users/lin/Desktop/gameProject/LiveActivity/ActivitySDK'
   
     
  //扩展Extension 添加SDK 
   target 'HZWLiveActivityExtension' do
  #    inherit! :search_paths
  pod  'ActivityUI',:git => "ssh://git@192.168.13.252:8022/modo_ios/activityui.git"
#  pod  'ActivityUI', :path => '../LiveActivity/ActivityUI'
end
   
2.//# 在pod 中强制更改为动态,目的暴露 Swift API(添加在能力SDK(被设置覆盖成为了静态库))
    
    /*为什么?  
    在pod中 下面RN库 覆盖全局 都是静态库,而swift开发的SDK,OC 是无法在静态库中引入 #import <ActivitySDK/ActivitySDK-Swift.h>   
    没有这个,我发使用SDK 所以强制动态,但是纯能力SDK 引入后,还是无法使用.
    需要在主工程中 动态引入  
   config = use_native_modules!
  use_react_native!(:path => config["reactNativePath"],:hermes_enabled => true)
    */
pre_install do |installer|
  installer.pod_targets.each do |pod|
    if pod.name == 'ActivitySDK'
      puts "👉 [pre_install] 强制将 #{pod.name} 设置为动态 framework"
      def pod.build_type
        Pod::BuildType.dynamic_framework
      end
    end
  end
end

          
//权限支持  考虑是不是要与通知权限一起判断
3.主APP
	<key>NSSupportsLiveActivities</key>
	<true/>
```



2. App Groups (共享容器)

```objc
// 写入共享 UserDefaults  "group.com.hzw"
        let sharedDefaults = UserDefaults(suiteName: "group.com." + pizzaDeliveryAttributes.gameImageName)
        sharedDefaults?.set(initialContentState.totalTime, forKey: "totalTime")
        sharedDefaults?.set(initialContentState.curTime, forKey: "curTime")
        sharedDefaults?.synchronize()
```

3. 编译demo 或者项目的时候 如果找不到-swift文件,可以尝试一个个的编译,先sdk,后能力sdk,在demo

4.目前ActivityAttributes

```
ActivityAttributes {
    public struct ContentState: Codable, Hashable {
        public  var liveActivity_name: String //eg:牡丹花
        public  var liveActivity_status_number:Int //1,种植中， 2,成熟
        public  var liveActivity_status_shortString: String //种植中:已成熟
        public  var liveActivity_status_longString: String //茁壮成长中! :已可采摘
        public  var liveActivity_plan_string: String //鲜花成熟倒计时 : 已成熟可采摘
        public  var liveActivity_countdown: TimeInterval //倒计时CD
        public  var liveActivity_timestamp: TimeInterval //倒计时时间戳
        public  var liveActivity_other: String //补充
```

```
 NSTimeInterval timestamp = [[NSDate date] timeIntervalSince1970] + 60; // 当前时间戳（秒）
        NSDictionary *dict = @{
            @"liveActivity_name": @"牡丹花",
            @"liveActivity_status_shortString": @"1",
            @"liveActivity_status_longString": @"茁壮成长中!",
            @"liveActivity_plan_string": @"鲜花成熟倒计时",
            @"liveActivity_status_number": @(1),
            @"liveActivity_countdown":@(100),
            @"liveActivity_timestamp": @(timestamp),
            @"liveActivity_button_name":@"前往花园"
        };
```

